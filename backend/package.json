{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && npx vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "encore.dev": "^1.52.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}